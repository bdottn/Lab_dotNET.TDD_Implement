namespace TDD.Arithnetic
{
    public class Division
    {
        public static decimal Divide(int dividend, int divisor)
        {
            decimal quotient =(decimal) dividend / (decimal)divisor;

            return quotient;
        }
    }
}